import 'package:flutter/cupertino.dart';

class Item {
  int id;
  String title;
  DateTime added;
  //DateTime dueDate;

  Item({
    required this.id,
    required this.title,
    required this.added,
  });
}
